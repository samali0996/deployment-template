apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  namespace: dev
  name: deploy-permissions
rules:
- apiGroups: ["", "apps"] # "" indicates the core API group
  resources: ["secrets", "services", "deployments", "replicasets"]
  verbs: ["get", "watch", "list", "create"]